services:
  db:
    image: postgres:17.5
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: loleslol24
      POSTGRES_DB: undersekai_db
    volumes:
      - ./data:/var/lib/postgresql/data